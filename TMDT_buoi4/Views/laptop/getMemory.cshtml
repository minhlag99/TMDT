@model IEnumerable<string>
    <div id="8">
        @foreach (var item in Model)
        {
            @Html.CheckBox(item);
            @Html.Label(item);
            <b>&ensp;</b>
        }
    </div>


